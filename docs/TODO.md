# OpsMind 改进清单

> 优先级：🔴 生产隐患 / 🟠 功能缺陷 / 🟡 架构债务 / 🟢 优化建议

---

# 后端

## 1. 智能问答与 RAG 管道

- 🟡 BM25 索引无增量更新，每次刷新全量重建——需算法重构
- 🟡 文档处理器无阶段内重试机制，embedding API 瞬时失败直接中止——需架构变更
- 🟢 RAG 历史截断按消息条数而非 token 数——设计权衡，非阻塞

## 2. 知识库管理

- 🟡 DOCX 解析仅读取 `word/document.xml`，不处理 `word/document2.xml` 分割文档
- 🟡 PDF/DOCX 解析前全量读入内存（`io.ReadAll`），大文件 OOM 风险
- 🟡 50MB 上传上限硬编码，不支持按 KB 粒度配置
- 🟡 MinIO 惰性下载——`GetObject` 成功不代表数据可读，`defer reader.Close()` 不检查错误
- 🟢 文档处理缺自动重试队列和死信队列——当前仅手动 `RetryDocument`

## 3. 数据看板

- 🟢 趋势查询窗口硬编码，不可配置——低优先级

---

# 前端

## 1. 智能问答

- 🟢 虚拟列表 `estimateSize: () => 80` 常量估算，变长消息滚动位置不准——需消息高度测量，非阻塞

## 2. 表单与交互

- 🟢 表单缺 required 标记——非阻塞
- 🟢 用户搜索无结果提示——非阻塞

## 3. 组件架构

- 🟢 StatusBadge 领域状态映射硬编码在组件内，后端新增状态时前端需同步更新——`statusText` prop 为已提供的逃生舱

## 4. 设计系统

- 🟢 审计页输入框样式在 5 处重复——审计页布局特殊，不适合直接 `AppleInput`

## 5. 基础设施

- 🟡 零代码分割，全量打包——需 `next/dynamic` 架构变更
- 🟢 全局 ErrorBoundary 仅顶层一个——`SectionErrorBoundary` 已覆盖内容区

---

## 统计

| | 🔴 P0 | 🟠 P1 | 🟡 P2 | 🟢 P3 |
|---|---|---|---|---|
| 后端 | 0 | 0 | 6 | 3 |
| 前端 | 0 | 0 | 1 | 6 |
| **合计** | **0** | **0** | **7** | **9** |

---

# 未来方向

## 产品路线图

### 2026 H2

- *本地Agent建立：支持在线深度搜索，智能化收集运维数据*
- 知识库模板系统：常见运维场景（新员工入职、设备申领）一键创建知识库结构
- 看板增强：自定义日期范围、数据导出
- 申告满意度评价：闭环反馈机制

### 2027

- 外部 ITSM 对接（Jira Service Management、ServiceNow）
- 自然语言转 SQL：非技术人员直接查询运维数据
- 知识库覆盖度分析：识别高频未命中问题，指导知识补充

## 技术演进

> 以下 6 项与上方待办对应，集中列出便于路线图视角：

- BM25 索引增量更新替代全量重建 → [后端 §1](#1-智能问答与-rag-管道)
- 文档处理器增加阶段内重试 + 死信队列 → [后端 §1](#1-智能问答与-rag-管道) + [§2](#2-知识库管理)
- DOCX 解析支持 `document2.xml` 分割文档 → [后端 §2](#2-知识库管理)
- PDF/DOCX 流式解析替代全量读入内存 → [后端 §2](#2-知识库管理)
- 前端代码分割：`next/dynamic` 按路由懒加载 → [前端 §5](#5-基础设施)
- 50MB 上传上限配置化 → [后端 §2](#2-知识库管理)
