# OpsMind M0 里程碑测试执行报告

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 执行日期 | 2026-05-15 |
| 执行人 | 测试负责人 |
| 里程碑 | M0 项目初始化与工程基线 |
| 环境 | Windows 10 Pro / Go 1.26.1 / pnpm 10.30.3 / Docker Desktop v2.39.2 |
| 代码版本 | b0c21b3 |
| 用例总数 | 6 |
| 通过数 | 6 |
| 失败数 | 0 |
| 阻塞数 | 0 |
| 回归结论 | 通过 |

## 1. 测试执行记录

### M0-TC001：检查目录结构 — 通过

**执行步骤：** 列出项目根目录，检查 `server/`、`web/`、`deploy/`、`docs/` 目录及关键文件。

**结果：**

| 目录/文件 | 状态 |
|-----------|------|
| `server/`、`server/cmd/opsmind/`、`server/configs/` | 存在 |
| `web/`、`web/src/` | 存在 |
| `deploy/`、`deploy/nginx/` | 存在 |
| `docs/`、`docs/API/`、`docs/FLOW/`、`docs/TEST/` | 存在 |
| `.env.example` | 存在 |
| `README.md` | 存在 |
| `.gitignore` | 存在 |
| `scripts/` | 不存在（M0 不要求） |

**备注：** `scripts/` 目录按 M6 交付，不在 M0 范围内。

---

### M0-TC002：后端基础启动 — 通过

**执行步骤：** `cd server && go run ./cmd/opsmind`，等待启动后请求健康检查。

**结果：**

```
GET /health → 200 {"service":"opsmind-server","status":"ok"}
GET /api/v1/ping → 200 {"message":"pong"}
```

服务无 panic，无编译错误，无端口冲突。Gin 路由注册正常。

---

### M0-TC003：前端基础启动 — 通过

**执行步骤：** `cd web && pnpm install --frozen-lockfile && npx vite build`

**结果：**

- `pnpm install` 安装 105 个包，无错误
- `vite build` 产出 `dist/index.html`、`dist/assets/index-*.css`、`dist/assets/index-*.js`
- TypeScript 类型检查通过（vue-tsc --noEmit）

---

### M0-TC004：依赖服务启动 — 通过

**执行步骤：** `docker compose -f deploy/docker-compose.yml up -d postgres redis nats minio`

**结果：**

| 容器 | 镜像 | 状态 |
|------|------|------|
| opsmind-postgres | pgvector/pgvector:pg18 | healthy |
| opsmind-redis | redis:7-alpine | healthy |
| opsmind-nats | nats:2.10-alpine | healthy |
| opsmind-minio | minio/minio:latest | healthy |

**修复记录：** PG18 镜像要求 volume 挂载到 `/var/lib/postgresql` 而非 `/var/lib/postgresql/data`，已在 `deploy/docker-compose.yml` 中修正。

---

### M0-TC005：环境变量检查 — 通过

**执行步骤：** 检查 `.env.example` 文件存在、所有必需配置项完整、无真实密钥泄露。

**结果：**

24 个配置项全部定义：`APP_ENV`、`SERVER_PORT`、`DB_HOST`、`DB_PORT`、`DB_USER`、`DB_PASSWORD`、`DB_NAME`、`DB_SSLMODE`、`REDIS_ADDR`、`REDIS_PORT`、`REDIS_PASSWORD`、`NATS_URL`、`NATS_PORT`、`MINIO_ENDPOINT`、`MINIO_PORT`、`MINIO_CONSOLE_PORT`、`MINIO_ACCESS_KEY`、`MINIO_SECRET_KEY`、`MINIO_BUCKET`、`MINIO_USE_SSL`、`ANYTHINGLLM_BASE_URL`、`ANYTHINGLLM_API_KEY`、`VLLM_BASE_URL`、`VLLM_API_KEY`、`VLLM_MODEL`、`JWT_SECRET`

无真实密钥、Token 或生产连接串泄露。所有值为占位符。

**修复记录：** 补充了 `REDIS_PORT`、`NATS_PORT`、`MINIO_PORT`、`MINIO_CONSOLE_PORT` 四个变量，与 `deploy/docker-compose.yml` 的 `${VAR:-default}` 引用对齐。

---

### M0-TC006：README 启动步骤验证 — 通过

**执行步骤：** 检查 README.md 是否包含项目结构、技术栈、环境要求、启动命令、配置步骤。

**结果：**

| 检查项 | 状态 |
|--------|------|
| 项目结构 | 含目录树和职责说明 |
| 技术栈 | 含前后端/数据库/缓存/队列/存储/AI 选型 |
| 环境要求 | 含 CPU/内存/磁盘/Go/Node/Docker 版本 |
| 配置说明 | 含 `cp .env.example .env` 和变量清单 |
| 本地启动 | 含依赖/后端/前端/完整 Docker 四步命令 |
| 验证命令 | 含 `docker compose ps` 和日志查看 |
| 文档入口 | 含 PRD/TECH/DB/API/FLOW/PLAN/TEST/DEPLOY 链接 |

## 2. 缺陷汇总

本里程碑无新增缺陷。

## 3. 遗留风险

| 风险 | 说明 | 应对 |
|------|------|------|
| Docker 镜像拉取 | 首次启动需从 Docker Hub 拉取 pgvector/pgvector:pg18 等镜像，网络不稳定时可能超时 | 已配置默认值兜底，可预先 `docker pull` |
| PG18 volume 路径 | pgvector/pgvector:pg18 要求 volume 挂载到 `/var/lib/postgresql` | 已修正，后续 PG 大版本升级注意检查 |

## 4. M0 验收结论

6 项 P0/P1 用例全部通过，0 个未关闭缺陷。后端、前端、Docker 依赖均可独立启动，README 可复现。M0 工程基线验收通过，可进入 M1 数据库与后端基础能力开发。
