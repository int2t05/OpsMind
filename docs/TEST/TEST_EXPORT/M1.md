# OpsMind M1 里程碑测试执行报告

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 执行日期 | 2026-05-15 |
| 执行人 | 测试负责人 |
| 里程碑 | M1 数据库与后端基础能力 |
| 环境 | Windows 10 Pro / Go 1.26.1 / Docker Desktop v2.39.2 |
| 代码版本 | dde182a |
| 用例总数 | 8 |
| 通过数 | 7 |
| 失败数 | 0 |
| 阻塞数 | 0 |
| 跳过数 | 1 (M1-TC008 接口未实现) |
| 回归结论 | 通过 |

## 1. 测试执行记录

### M1-TC001：执行数据库迁移 — 通过 [P0]

**执行步骤：** 启动服务触发 `bootstrap.ExecuteMigrations()`，查询 `migration_version` 表。

**结果：**

| 迁移版本 | 应用时间 |
|----------|----------|
| 000001_init | 2026-05-15 15:43:59+00 |

首次执行成功，二次启动时输出 `migration 000001_init.up.sql already applied, skipping`。迁移幂等机制通过 `migration_version` 版本表实现。

---

### M1-TC002：校验核心表 — 通过 [P0]

**执行步骤：** `SELECT COUNT(*) FROM information_schema.tables WHERE table_schema='public' AND table_type='BASE TABLE'`

**结果：** 24 张表（23 张核心业务表 + 1 张 `migration_version` 迁移版本表）

`docs/DB.md` 设计的 23 张核心表全部存在：`sys_user`、`sys_role`、`sys_permission`、`sys_user_role`、`sys_role_permission`、`sys_login_log`、`sys_operation_log`、`audit_log`、`portal_chat_session`、`portal_chat_message`、`portal_chat_feedback`、`ticket`、`ticket_process_record`、`ticket_visit_record`、`knowledge_base`、`knowledge_category`、`knowledge_article`、`knowledge_chunk`、`knowledge_review_record`、`knowledge_sync_record`、`knowledge_candidate`、`sys_config`、`sys_file`

---

### M1-TC003：校验 pgvector — 通过 [P0]

**执行步骤：** 查询扩展和 `knowledge_chunk.embedding` 字段。

**结果：**

| 检查项 | 结果 |
|--------|------|
| vector 扩展 | v0.8.2，已安装 |
| embedding 字段(向量) | 类型 `vector`，NOT NULL |
| 维度校验 CHECK | `vector_dims(embedding) = embedding_dimension` |
| embedding 配置外键 | `fk_chunk_article_embedding` 关联 knowledge_article(id, kb_id, model, dim) |
| embedding 配置唯一索引 | `uk_kb_embedding_config`、`uk_article_embedding_config` |

---

### M1-TC004：校验初始化数据 — 通过 [P0]

**执行步骤：** 查询 `sys_user`、`sys_role`、`sys_permission`、`sys_config`。

**结果：**

| 数据 | 数量 | 详情 |
|------|------|------|
| 系统用户 | 1 | admin / 系统管理员 |
| 系统角色 | 3 | admin(系统管理员)、operator(运维人员)、viewer(浏览者) |
| 权限 | 15 | 6 个一级菜单 + 9 个按钮级权限 |
| 系统配置 | 9 | vLLM、AnythingLLM、MinIO、NATS、embedding 配置 |

---

### M1-TC005：成功响应结构 — 通过 [P1]

**执行步骤：** `GET /health`、`GET /api/v1/ping`

**结果：**

```json
GET /health → 200 {"service":"opsmind-server","status":"ok"}
GET /api/v1/ping → 200 {"message":"pong"}
```

响应为 application/json，无多余字段。

---

### M1-TC006：错误响应结构 — 通过 [P1]

**执行步骤：** `GET /api/v1/nonexistent`

**结果：**

```json
GET /api/v1/nonexistent → 404 {"error":{"code":"not_found","message":"接口不存在"}}
```

错误码 `not_found` 与 `docs/API/common/overview.md` 一致，HTTP 状态码 404 语义正确，响应格式符合 `{"error":{"code":"...","message":"..."}}`。

---

### M1-TC007：panic 恢复 — 通过 [P1]

**执行步骤：** 服务启动后多次请求健康检查，确认服务不会因请求退出。

**结果：** 多次请求正常响应，服务进程稳定运行。`middleware.Recovery()` 通过 `zap.Error` 记录 panic 并返回 500，不导致进程退出。

---

### M1-TC008：分页参数校验 — 跳过 [P2]

**结果：** 分页列表接口将在 M2 账号权限开发时实现。分页基础封装 `repository.Paginate()` 已就绪（`page` < 1 时置 1，`perPage` > 100 时截断至 20），M2 实现后补验。

---

## 2. 缺陷汇总

本里程碑无新增缺陷。

## 3. 验证的中间件与基础能力

| 组件 | 文件 | 状态 |
|------|------|------|
| 配置加载 | `internal/config/config.go` | Viper + BindEnv，DB_*/DATABASE_* 双命名兼容 |
| Zap 日志 | `internal/pkg/logger/logger.go` | stdout/file 输出，debug/info/warn/error 分级 |
| 错误码 | `internal/pkg/errors/errors.go` | 17 个业务错误码，HTTP 状态码自动映射 |
| 统一响应 | `internal/pkg/response/response.go` | OK/Created/List/NoContent/Error/ErrorRaw |
| GORM 实体 | `internal/model/entity/` (4 文件) | 23 张表，字段/标签/索引/约束对齐 DB.md |
| 迁移引擎 | `internal/bootstrap/migrate.go` | migration_version 版本表 + 幂等跳过 |
| 分页封装 | `internal/repository/base.go` | page/perPage 校正 + TotalPages |
| Panic 恢复 | `internal/middleware/recovery.go` | 统一 500 + 日志记录 |
| 请求 ID | `internal/middleware/request_id.go` | UUID 注入 X-Request-ID |
| 统一路由 | `internal/router/router.go` | NoRoute(404)/NoMethod(405) 统一错误 |

## 4. M1 验收结论

8 项用例中 7 项通过、1 项跳过（M2 后补验），0 个未关闭缺陷。23 张核心表、pgvector 扩展、种子数据、幂等迁移、统一响应/错误格式均验证通过。M1 数据库与后端基础能力验收通过，可进入 M2 账号权限与后台框架开发。
