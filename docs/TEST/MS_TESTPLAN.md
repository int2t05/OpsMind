# 运维数字员工系统里程碑测试计划

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 生成日期 | 2026-05-15 |
| 覆盖里程碑 | M0-M6 |
| 计划依据 | `docs/PLAN.md` |

## 1. M0 项目初始化与工程基线测试计划

测试目标：验证工程目录、前后端基础工程、依赖服务编排、环境变量和 README 初版可用。

测试重点：

| 重点 | 说明 |
|------|------|
| 工程结构 | `server/`、`web/`、`deploy/`、`scripts/`、`docs/` 目录清晰 |
| 后端启动 | Go 服务可启动并返回健康检查 |
| 前端启动 | Vue 应用可通过 Vite 启动 |
| Docker 依赖 | PostgreSQL、Redis、NATS、MinIO 可启动 |
| 配置安全 | `.env.example` 无真实密钥，配置项完整 |

测试用例：

| 编号 | 用例 | 前置条件 | 执行步骤 | 预期结果 | 缺陷等级 |
|------|------|----------|----------|----------|----------|
| M0-TC001 | 检查目录结构 | 拉取最新代码 | 查看 `server/`、`web/`、`deploy/`、`scripts/`、`docs/` | 目录存在且职责与 `docs/PLAN.md` 一致 | P2 |
| M0-TC002 | 后端基础启动 | Go 环境可用 | 在 `server/` 执行 `go run ./cmd/opsmind` | 服务启动，无 panic，健康检查可访问 | P0 |
| M0-TC003 | 前端基础启动 | Node、pnpm 可用 | 在 `web/` 执行 `pnpm install`、`pnpm dev` | 浏览器可打开前端空壳页面 | P0 |
| M0-TC004 | 依赖服务启动 | Docker 可用 | 执行 `docker compose up -d postgres redis nats minio` | 四个依赖容器均为 running | P0 |
| M0-TC005 | 环境变量检查 | `.env.example` 存在 | 检查数据库、Redis、NATS、MinIO、RAG、模型服务配置项 | 配置项完整，无真实密码和密钥 | P1 |
| M0-TC006 | README 启动步骤验证 | README 初版存在 | 按 README 在干净环境执行启动步骤 | 步骤可复现，无缺失命令 | P1 |

通过标准：

| 标准 | 要求 |
|------|------|
| P0 缺陷 | 0 个未关闭 |
| 启动链路 | 后端、前端、依赖服务均可启动 |
| 文档 | README 至少包含项目结构和本地启动方式 |

## 2. M1 数据库与后端基础能力测试计划

测试目标：验证数据库迁移、初始化数据、统一响应、统一错误、日志、基础路由和中间件可用。

测试重点：

| 重点 | 说明 |
|------|------|
| 数据库迁移 | 23 张核心表、索引、约束和 pgvector 扩展 |
| 初始化数据 | 默认管理员、角色、权限码、系统配置 |
| 统一响应 | 成功响应 `data/meta`，错误响应 `error` |
| 错误映射 | 400、401、403、404、409、422、429、502、503 语义正确 |
| 日志链路 | 请求 ID、结构化日志、panic 恢复 |

测试用例：

| 编号 | 用例 | 前置条件 | 执行步骤 | 预期结果 | 缺陷等级 |
|------|------|----------|----------|----------|----------|
| M1-TC001 | 执行数据库迁移 | PostgreSQL 启动 | 执行迁移命令 | 所有迁移成功，无错误中断 | P0 |
| M1-TC002 | 校验核心表 | 迁移完成 | 查询 `information_schema.tables` | `docs/DB.md` 中 23 张核心表存在 | P0 |
| M1-TC003 | 校验 pgvector | 迁移完成 | 查询扩展和 `knowledge_chunk.embedding` 字段 | `vector` 扩展存在，向量字段可用 | P0 |
| M1-TC004 | 校验初始化管理员 | seed 完成 | 查询 `sys_user`、`sys_role`、`sys_permission` | 默认管理员、角色和权限码存在 | P0 |
| M1-TC005 | 成功响应结构 | 服务启动 | 请求健康检查或示例查询接口 | 返回 JSON，成功数据包在 `data` 中 | P1 |
| M1-TC006 | 错误响应结构 | 服务启动 | 发送非法 JSON 或非法参数 | 返回 `error.code`、`error.message`、`details` | P1 |
| M1-TC007 | panic 恢复 | 开发测试路由可用 | 触发测试 panic | 返回 500，不导致服务退出，日志含 request_id | P1 |
| M1-TC008 | 分页参数校验 | 列表接口可用 | 请求 `page=0` 或 `per_page=101` | 返回 `validation_error` | P2 |

通过标准：

| 标准 | 要求 |
|------|------|
| 数据迁移 | 可重复执行或有明确版本控制 |
| 基础表 | 表、索引、外键、唯一约束符合 DB 文档 |
| 错误结构 | 与 `docs/API/common/overview.md` 一致 |

## 3. M2 账号权限与后台框架测试计划

测试目标：验证后台登录、Token、账号管理、角色权限、前端路由守卫、动态菜单和接口权限控制。

测试重点：

| 重点 | 说明 |
|------|------|
| 登录认证 | 正确账号登录、错误密码、冻结账号、Token 过期 |
| 权限加载 | 当前用户、菜单权限、接口权限 |
| 账号管理 | 创建、查询、详情、更新、冻结、恢复 |
| 角色权限 | 角色列表、权限树、角色权限更新 |
| 前端守卫 | 未登录跳转登录页，无权限隐藏菜单和按钮 |

测试用例：

| 编号 | 用例 | 前置条件 | 执行步骤 | 预期结果 | 缺陷等级 |
|------|------|----------|----------|----------|----------|
| M2-TC001 | 管理员登录成功 | 管理员账号存在 | 调用 `POST /api/v1/auth/login` | 返回 `access_token`、用户信息、权限 | P0 |
| M2-TC002 | 密码错误登录失败 | 管理员账号存在 | 使用错误密码登录 | 返回认证错误，写入失败登录日志 | P1 |
| M2-TC003 | 冻结账号不可登录 | 冻结账号存在 | 使用冻结账号登录 | 返回 `account_frozen` 或同类错误 | P0 |
| M2-TC004 | 获取当前用户 | 已登录 | 调用 `GET /api/v1/auth/profile` | 返回当前用户基础信息和角色 | P1 |
| M2-TC005 | 获取当前权限 | 已登录 | 调用 `GET /api/v1/auth/permissions` | 返回菜单、按钮和接口权限 | P1 |
| M2-TC006 | 创建账号 | 管理员登录 | 调用 `POST /api/v1/admin/accounts` | 新账号创建成功，用户名唯一 | P1 |
| M2-TC007 | 冻结账号 | 管理员登录 | 调用 `POST /api/v1/admin/accounts/{id}/freeze` | 账号状态为冻结，写入审计日志 | P1 |
| M2-TC008 | 恢复账号 | 管理员登录 | 调用 `POST /api/v1/admin/accounts/{id}/restore` | 账号状态恢复正常，写入审计日志 | P1 |
| M2-TC009 | 无权限访问接口 | 普通账号登录 | 访问账号管理接口 | 返回 `403 forbidden` | P0 |
| M2-TC010 | 前端路由守卫 | 未登录浏览器 | 访问后台首页 | 跳转登录页 | P1 |
| M2-TC011 | 动态菜单 | 不同角色登录 | 查看后台菜单 | 菜单与权限一致 | P1 |

通过标准：

| 标准 | 要求 |
|------|------|
| 登录链路 | 成功、失败、冻结、退出均可验证 |
| 权限控制 | 前端展示和后端鉴权同时生效 |
| 审计 | 账号冻结、恢复、角色权限修改有记录 |

## 4. M3 门户问答与申告闭环测试计划

测试目标：验证门户问答、反馈、匿名附件上传、申告创建和申告查询安全。

测试重点：

| 重点 | 说明 |
|------|------|
| 问答接口 | `POST /api/v1/portal/chat-sessions` |
| 反馈接口 | `POST /api/v1/portal/chat-sessions/{session_no}/feedback` |
| 附件上传 | `POST /api/v1/portal/files` |
| 申告创建 | `POST /api/v1/portal/tickets` |
| 申告查询 | `GET /api/v1/portal/tickets/{ticket_no}` |
| 异常兜底 | RAG、模型、附件、限流、手机号校验 |

测试用例：

| 编号 | 用例 | 前置条件 | 执行步骤 | 预期结果 | 缺陷等级 |
|------|------|----------|----------|----------|----------|
| M3-TC001 | 门户问答成功 | 默认知识库可用 | 提交“账号冻结怎么处理” | 返回答案、来源、置信度、`answered` | P0 |
| M3-TC002 | 问题为空 | 服务启动 | 提交空 `question` | 返回 `validation_error` | P1 |
| M3-TC003 | 知识库不存在 | 服务启动 | 使用无效 `knowledge_base_code` | 返回 `knowledge_base_not_found` | P1 |
| M3-TC004 | RAG 不可用兜底 | 停止 AnythingLLM 或使用 mock 失败 | 提交问题 | 返回可转人工状态或 `rag_unavailable` | P0 |
| M3-TC005 | 模型不可用兜底 | 停止 vLLM 或使用 mock 失败 | 提交问题 | 返回可转人工状态或 `model_unavailable` | P0 |
| M3-TC006 | 提交已解决反馈 | 已有问答会话 | 提交 `resolved` | 反馈写入 `portal_chat_feedback` | P1 |
| M3-TC007 | 提交未解决反馈 | 已有问答会话 | 提交 `unresolved` | 反馈写入并前端展示申告入口 | P1 |
| M3-TC008 | 门户上传合法附件 | 准备 PNG 文件 | 调用 `POST /api/v1/portal/files` | 返回 `file_id` 和 MinIO 路径 | P1 |
| M3-TC009 | 上传超大附件 | 准备超过 20 MB 文件 | 调用上传接口 | 返回 `file_too_large` | P1 |
| M3-TC010 | 上传非法类型 | 准备可执行文件 | 调用上传接口 | 返回 `unsupported_file_type` | P1 |
| M3-TC011 | 创建申告成功 | 附件已上传 | 提交申告表单 | 返回 `ticket_no`、`pending` | P0 |
| M3-TC012 | 创建申告缺必填 | 服务启动 | 不传 `reporter_phone` | 返回 `validation_error` | P1 |
| M3-TC013 | 查询申告成功 | 申告已创建 | 使用正确手机号查询 | 返回申告状态、处理记录、回访记录 | P1 |
| M3-TC014 | 手机号不匹配 | 申告已创建 | 使用错误手机号查询 | 返回 `contact_mismatch` | P0 |
| M3-TC015 | 匿名接口限流 | Redis 可用 | 高频提交问答或附件 | 超阈值返回 `rate_limit_exceeded` | P1 |

通过标准：

| 标准 | 要求 |
|------|------|
| 门户主路径 | 问答、反馈、上传、申告、查询全链路通过 |
| 匿名安全 | 限流、文件限制、手机号校验生效 |
| 兜底 | RAG/模型失败时不阻断申告流转 |

## 5. M4 后台申告处理与知识沉淀测试计划

测试目标：验证后台申告处理、状态机、回访、知识候选、知识库、知识条目审核发布。

测试重点：

| 重点 | 说明 |
|------|------|
| 申告列表和详情 | 筛选、分页、问答上下文、附件、处理记录 |
| 状态流转 | `pending`、`processing`、`need_more_info`、`completed`、`closed` |
| 回访记录 | 已完成申告登记回访 |
| 知识候选 | 已完成申告生成候选且唯一 |
| 知识审核发布 | 草稿、待审核、审核通过、发布、停用 |
| embedding 约束 | 知识库模型和维度锁定 |

测试用例：

| 编号 | 用例 | 前置条件 | 执行步骤 | 预期结果 | 缺陷等级 |
|------|------|----------|----------|----------|----------|
| M4-TC001 | 查询申告列表 | 运维人员登录 | 调用 `GET /api/v1/admin/tickets` | 返回分页列表和 meta | P1 |
| M4-TC002 | 按状态筛选 | 存在多状态申告 | 使用 `status=pending` 查询 | 只返回待处理申告 | P2 |
| M4-TC003 | 获取申告详情 | 申告存在 | 调用 `GET /api/v1/admin/tickets/{id}` | 返回上下文、附件、处理记录 | P1 |
| M4-TC004 | 开始处理 | 申告为 `pending` | 更新为 `processing` | 状态更新，写入处理记录 | P0 |
| M4-TC005 | 非法状态流转 | 申告为 `pending` | 直接更新为 `completed` 且不填过程 | 返回 `invalid_status_transition` 或校验错误 | P0 |
| M4-TC006 | 完成处理缺结果 | 申告为 `processing` | 更新为 `completed` 但不填 `process_result` | 返回 `validation_error` | P1 |
| M4-TC007 | 完成处理成功 | 申告为 `processing` | 填写处理过程和结果 | 状态为 `completed` | P0 |
| M4-TC008 | 新增回访记录 | 申告已完成 | 调用 `POST /api/v1/admin/tickets/{id}/visit-records` | 回访记录创建成功 | P1 |
| M4-TC009 | 未完成申告回访 | 申告未完成 | 新增回访记录 | 返回 `invalid_ticket_status` | P1 |
| M4-TC010 | 生成知识候选 | 申告已完成 | 调用 `POST /api/v1/admin/tickets/{id}/knowledge-candidate` | 返回候选 ID，状态 `pending_review` | P0 |
| M4-TC011 | 重复生成知识候选 | 候选已存在 | 再次生成 | 返回冲突或已处理错误 | P1 |
| M4-TC012 | 创建知识库 | 管理员登录 | 创建 `ops-faq` 知识库 | 状态 `active`，embedding 配置保存 | P1 |
| M4-TC013 | 创建知识条目 | 知识库和分类存在 | 调用 `POST /api/v1/admin/knowledge-articles` | 条目为 `draft` | P1 |
| M4-TC014 | 提交审核 | 知识条目为 `draft` | 调用 submit-review | 状态为 `pending_review` | P1 |
| M4-TC015 | 审核通过 | 条目为 `pending_review` | 调用 review approved | `review_status=approved`，状态仍为 `pending_review` | P1 |
| M4-TC016 | 发布知识 | 审核通过 | 调用 publish | 状态为 `published`，同步状态 `syncing` | P0 |
| M4-TC017 | 停用知识 | 条目已发布 | 调用 disable | 状态为 `disabled` | P1 |
| M4-TC018 | 修改锁定 embedding | 知识库已有发布知识 | 修改模型或维度 | 返回 `embedding_config_locked` | P0 |

通过标准：

| 标准 | 要求 |
|------|------|
| 状态机 | 合法流转通过，非法流转拒绝 |
| 知识沉淀 | 完成申告可转候选、候选可转知识、知识可发布 |
| 数据一致 | 处理记录、回访、候选、审核记录可追溯 |

## 6. M5 AI/RAG 同步、看板、配置与审计测试计划

测试目标：验证外部服务适配、知识同步 Worker、配置管理、看板指标、日志审计。

测试重点：

| 重点 | 说明 |
|------|------|
| NATS 事件 | 发布知识生成 `knowledge.published` 事件 |
| Worker 同步 | 同步 AnythingLLM、写入 pgvector、记录同步状态 |
| 系统配置 | 配置查询、详情、更新、连接测试、模型选项 |
| 看板 | 摘要、趋势、待办统计 |
| 审计日志 | 审计、操作、登录日志筛选和导出 |

测试用例：

| 编号 | 用例 | 前置条件 | 执行步骤 | 预期结果 | 缺陷等级 |
|------|------|----------|----------|----------|----------|
| M5-TC001 | 发布事件生成 | 知识审核通过 | 调用 publish | NATS 收到 `knowledge.published` 事件 | P0 |
| M5-TC002 | Worker 同步成功 | AnythingLLM、pgvector 可用 | 发布知识并等待 Worker | `rag_sync_status=success`，同步记录成功 | P0 |
| M5-TC003 | Worker 同步失败 | AnythingLLM 不可用 | 发布知识并等待 Worker | `rag_sync_status=failed`，记录错误信息 | P1 |
| M5-TC004 | 向量维度校验 | 构造错误维度 | 执行同步 | 拒绝写入并记录失败原因 | P0 |
| M5-TC005 | 查询系统配置 | 管理员登录 | 调用 `GET /api/v1/admin/configs` | 返回分页配置列表 | P1 |
| M5-TC006 | 更新系统配置 | 管理员登录 | 修改 RAG 地址或模型参数 | 配置更新，写入审计日志 | P1 |
| M5-TC007 | 测试连接成功 | 外部服务可用 | 调用 `POST /api/v1/admin/configs/test-connections` | 返回连接成功状态 | P1 |
| M5-TC008 | 测试连接失败 | 外部服务不可用 | 调用连接测试 | 返回失败原因，不写入错误配置 | P1 |
| M5-TC009 | 查询看板摘要 | 存在问答和申告数据 | 调用 dashboard summary | 返回问答量、解决率、人工转派量等 | P2 |
| M5-TC010 | 查询问答趋势 | 存在多日数据 | 调用 chat-trends | 返回按日统计数组 | P2 |
| M5-TC011 | 查询审计日志 | 存在敏感操作 | 调用 audit-logs | 可按模块、动作筛选 | P1 |
| M5-TC012 | 导出审计日志 | 管理员登录 | 调用 audit export | 返回导出文件或任务信息 | P2 |
| M5-TC013 | 查询登录日志 | 存在登录记录 | 调用 login-logs | 成功和失败登录均可查询 | P1 |

通过标准：

| 标准 | 要求 |
|------|------|
| 同步链路 | 成功和失败均可追踪 |
| 配置管理 | 连接测试可用，敏感配置不明文泄露 |
| 运营数据 | 看板、审计、登录日志可用于演示 |

## 7. M6 联调、测试、部署与验收测试计划

测试目标：验证完整端到端闭环、自动化测试、部署脚本、演示数据和验收材料。

测试重点：

| 重点 | 说明 |
|------|------|
| 全链路 E2E | 门户问答 -> 申告 -> 后台处理 -> 知识发布 -> 再问答 |
| 自动化测试 | 后端 `go test ./...`、前端构建、Playwright 冒烟 |
| 部署验证 | Docker Compose 完整启动 |
| 演示数据 | seed 数据可重复初始化 |
| 文档验收 | README、DEPLOY、TEST、API、FLOW、PLAN 完整 |

测试用例：

| 编号 | 用例 | 前置条件 | 执行步骤 | 预期结果 | 缺陷等级 |
|------|------|----------|----------|----------|----------|
| M6-TC001 | 后端全量测试 | 后端代码完成 | 执行 `go test ./...` | 全部通过 | P0 |
| M6-TC002 | 前端构建 | 前端代码完成 | 执行 `pnpm build` | 构建成功，无类型错误 | P0 |
| M6-TC003 | E2E 门户问答 | 测试环境启动 | 执行 Playwright 问答用例 | 问答成功，来源展示正常 | P0 |
| M6-TC004 | E2E 申告闭环 | 测试环境启动 | 问答未解决后创建申告 | 返回申告编号并可查询 | P0 |
| M6-TC005 | E2E 后台处理 | 申告已创建 | 运维人员登录并完成处理 | 状态为 `completed`，有处理记录 | P0 |
| M6-TC006 | E2E 知识发布 | 申告已完成 | 生成候选、转知识、审核、发布 | 知识状态 `published` | P0 |
| M6-TC007 | Docker 完整启动 | Docker 可用 | 执行 `docker compose up -d` | 前端、后端、依赖服务均可访问 | P0 |
| M6-TC008 | 演示数据重置 | seed 脚本存在 | 执行 `scripts/seed.sql` | 演示账号、知识、申告数据恢复 | P1 |
| M6-TC009 | README 复现 | 干净环境 | 按 README 启动系统 | 可登录并完成核心演示 | P1 |
| M6-TC010 | OpenAPI 对齐 | 接口实现完成 | 对比 `docs/API/openapi.yaml` 和实际路由 | 主要路径、方法、状态码一致 | P1 |
| M6-TC011 | 文档完整性 | 文档归档前 | 检查 PRD、TECH、DB、API、FLOW、PLAN、TEST、DEPLOY | 文档齐全且无旧 API 路径 | P2 |

通过标准：

| 标准 | 要求 |
|------|------|
| P0/P1 缺陷 | 全部关闭 |
| 核心闭环 | E2E 手工或自动化通过 |
| 部署 | Docker Compose 环境可复现 |
| 文档 | 关键文档完整且路径一致 |

## 8. 回归测试清单

| 场景 | 回归时机 | 检查点 |
|------|----------|--------|
| 登录权限 | 修改认证、账号、角色、菜单时 | 登录、冻结、无权限、菜单展示 |
| 门户问答 | 修改 RAG、模型、知识库时 | 成功问答、失败兜底、反馈 |
| 申告创建 | 修改门户表单、文件、ticket 表时 | 上传、创建、查询、手机号校验 |
| 申告处理 | 修改状态机或后台详情时 | 状态流转、处理记录、回访 |
| 知识发布 | 修改知识库、Worker、pgvector 时 | 审核、发布、同步成功/失败 |
| 审计日志 | 修改中间件或敏感操作时 | 操作记录、筛选、导出 |
| 部署配置 | 修改 Docker、env、Nginx 时 | 启动、健康检查、前端 API 代理 |

## 9. 测试执行记录模板

| 字段 | 内容 |
|------|------|
| 执行日期 | YYYY-MM-DD |
| 执行人 | 测试负责人 |
| 里程碑 | M0-M6 |
| 环境 | 本地/测试服务器 |
| 代码版本 | commit hash |
| 用例总数 | 数量 |
| 通过数 | 数量 |
| 失败数 | 数量 |
| 阻塞数 | 数量 |
| 新增缺陷 | 缺陷编号列表 |
| 回归结论 | 通过/不通过 |
| 备注 | 遗留风险和下一步 |
